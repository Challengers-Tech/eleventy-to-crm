# EspoCRM API Configuration
# ⚠️ WARNING: NEVER commit real credentials to Git! ⚠️
# Copy this file to .env.local and fill in your actual credentials
# The .env.local file is already in .gitignore and will not be committed

# EspoCRM Instance URL
ESPOCRM_URL=https://crm.challengers.tech

# EspoCRM Authentication - Option 1: API Key (Recommended)
# To generate an API key in EspoCRM:
# 1. Go to Administration > API Users
# 2. Create a new API user or edit existing
# 3. Generate an API key
ESPOCRM_API_KEY=your_api_key_here

# EspoCRM Authentication - Option 2: Username/Password (Alternative)
# Use this if you don't have an API key
ESPOCRM_USERNAME=your_username
ESPOCRM_PASSWORD=your_password

# Note: These environment variables need to be set in your Netlify dashboard
# Go to: Site settings > Build & deploy > Environment variables
#
# API Key authentication is preferred as it's more secure and easier to manage
